%% 定义初始条件
omega=zeros(Y+1,X+1);omega2=zeros(Y+1,X+1);%存储涡量
omega_t=zeros(Y+1,X+1);%存储涡量的时间导数
u=zeros(Y+1,X+1);v=zeros(Y+1,X+1);%存储速度，同时定义内部初始速度为0
psi=zeros(Y+1,X+1);psi2=zeros(Y+1,X+1);%存储流函数
%这里保持最外面一圈固定，矩阵和坐标在图形上对应，需要特殊定义的只有最上方的速度以及各边涡量
% u(1,2:end)=1;%最上面一层速度为1，不包括上方左侧角点
% omega(1,2:end)=(u(1,2:end)-u(2,2:end))/dy^2;
u(1,2:end-1)=1;%最上面一层速度为1，不包括上方左右角点
omega(1,2:end-1)=(u(1,2:end-1)-u(2,2:end-1))/dy^2;
